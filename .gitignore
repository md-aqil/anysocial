node_modules/
dist/
.env
.env.local
*.log
.DS_Store
coverage/
.vscode/
.idea/

# Uploaded media files — user-generated, stored in PostMedia, never git-tracked
frontend/public/uploads/

# Gemini / AI workflow files — local workspace, not repo code
Gemini/

# One-off / VPS documents
VPS_POLICY.md
fix_platforms.py

# Scratch / temp work — never commit
scratch/

# Secrets
*-*.json
*.json
!package.json
!tsconfig.json
!next.config.json

# Cloudflare tunnel + VPS credentials (never commit)
.cloudflared/
tunnel-config/
*.env
*credentials.json

# Next.js build output
frontend/.next/
.next/
tunnel-config/credentials.json
